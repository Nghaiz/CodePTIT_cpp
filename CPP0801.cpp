#include <bits/stdc++.h>
#define ll long long
#define MOD 1000000007

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    ifstream in;
    in.open("PTIT.in");
    ofstream out;
    out.open("PTIT.out");

    string s;
    while (getline(in, s)){
        out << s << "\n";
    }
    return 0;
}